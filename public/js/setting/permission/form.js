import * as _ from "/js/setting/permission/_var.js";

_.name.textbox({
    label: 'Name',
    labelPosition: 'top',
    required: true,
    disabled: true,
});

_.guard_name.textbox({
    label: 'Guard Name',
    labelPosition: 'top',
    required: true,
    disabled: true,
});