import * as _ from "/js/setting/role/_var.js";

_.name.textbox({
    label: 'Name',
    labelPosition: 'top',
    required: true,
    disabled: true,
});

_.guard_name.textbox({
    label: 'Guard Name',
    labelPosition: 'top',
    required: true,
    disabled: true,
});